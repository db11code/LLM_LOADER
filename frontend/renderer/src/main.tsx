import React from 'react';
import ReactDOM from 'react-dom/client';
import ModelPicker from './pages/Home/ModelPicker';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ModelPicker />
  </React.StrictMode>,
); 